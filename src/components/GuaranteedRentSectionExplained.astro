---
import { iconMap } from '../icons/iconMap';
---

<section class="guaranteed-rent" id="rentabilidade-garantida">
  <div class="container">
    <div class="section-header">
      <div class="header-badge">
        <div class="badge-icon" set:html={iconMap.shield} />
        Rentabilidade Garantida
      </div>
      <h2>Durma Descansado. O Seu Rendimento Está Garantido.</h2>
      <p>Sem preocupações com vacância, sem surpresas desagradáveis. Apenas um valor fixo na sua conta todos os meses, como um relógio.</p>
    </div>

    <!-- Hero Visual -->
    <div class="hero-visual">
      <div class="visual-card main-card">
        <div class="card-header">
          <span class="card-label">O Seu Rendimento Mensal</span>
          <div class="guaranteed-badge">
            <div class="badge-shield-icon" set:html={iconMap.shield} />
            100% Garantido
          </div>
        </div>
        <div class="amount-display">
          <span class="currency">€</span>
          <span class="amount" id="animatedAmount">1.200</span>
          <span class="period">/mês</span>
        </div>
        <div class="payment-info">
          <div class="info-item">
            <div class="info-icon" set:html={iconMap.check} />
            <span>Independente da ocupação</span>
          </div>
          <div class="info-item">
            <div class="info-icon" set:html={iconMap.check} />
            <span>Pagamento mensal previsível</span>
          </div>
          <div class="info-item">
            <div class="info-icon" set:html={iconMap.check} />
            <span>Contratos de 3 a 15 anos</span>
          </div>
        </div>
      </div>

      <div class="visual-stats">
        <div class="stat-card">
          <div class="stat-icon green" set:html={iconMap.chartLine} />
          <div class="stat-content">
            <span class="stat-value">Risco de Ocupação Assumido</span>
            <span class="stat-label">Rendimento Independente da Ocupação</span>
          </div>
        </div>
        <div class="stat-card">
          <div class="stat-icon orange" set:html={iconMap.euro} />
          <div class="stat-content">
            <span class="stat-value">Manutenção Incluída</span>
            <span class="stat-label">Manutenções Preventivas ao Imóvel</span>
          </div>
        </div>
      </div>
    </div>

    <!-- How it Works -->
    <div class="how-it-works">
      <h3>Como Funciona o Modelo de Rentabilidade Garantida?</h3>
      <div class="steps-grid">
        <div class="step-card">
          <div class="step-number">1</div>
          <div class="step-content">
            <h4>Análise do Imóvel</h4>
            <p>Analisamos o seu imóvel com base nas fotografias, planta, localização e características enviadas. Utilizamos dados de mercado e histórico operacional para avaliar o potencial e definir um valor de renda sustentável.</p>
          </div>
          <div class="step-arrow" set:html={iconMap.arrowRight} />
        </div>

        <div class="step-card">
          <div class="step-number">2</div>
          <div class="step-content">
            <h4>Proposta Clara e Personalizada</h4>
            <p>Apresentamos uma proposta transparente com o valor de renda mensal fixa, condições do modelo e duração do contrato, ajustada ao perfil do imóvel.</p>
          </div>
          <div class="step-arrow" set:html={iconMap.arrowRight} />
        </div>

        <div class="step-card">
          <div class="step-number">3</div>
          <div class="step-content">
            <h4>Contrato de Rentabilidade Garantida</h4>
            <p>Todos os meses, no mesmo dia, recebe o valor acordado na sua conta. Um fluxo de caixa estável, simples e previsível.</p>
          </div>
          <div class="step-arrow" set:html={iconMap.arrowRight} />
        </div>

        <div class="step-card">
          <div class="step-number">4</div>
          <div class="step-content">
            <h4>Recebimento Mensal Previsível</h4>
            <p>Todos os meses, no mesmo dia, recebe o valor acordado na sua conta. Simples, previsível, garantido.</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Comparison -->
    <div class="comparison-section">
      <h3>Rentabilidade Garantida vs Risco de Mercado</h3>
      <div class="comparison-grid">
        <div class="comparison-card left">
          <div class="comparison-header traditional">
            <div class="comparison-icon" set:html={iconMap.alert} />
            <h4>Modelo Tradicional</h4>
          </div>
          <ul class="comparison-list negative">
            <li>
              <div class="list-icon" set:html={iconMap.close} />
              Rendimento variável mês a mês
            </li>
            <li>
              <div class="list-icon" set:html={iconMap.close} />
              Períodos de vacância sem receita
            </li>
            <li>
              <div class="list-icon" set:html={iconMap.close} />
              Custos de manutenção imprevisíveis
            </li>
            <li>
              <div class="list-icon" set:html={iconMap.close} />
              Dificuldade em planear financeiramente
            </li>
            <li>
              <div class="list-icon" set:html={iconMap.close} />
              Envolvimento constante na gestão
            </li>
          </ul>
        </div>

        <div class="comparison-divider">
          <span>VS</span>
        </div>

        <div class="comparison-card right">
          <div class="comparison-header guaranteed">
            <div class="comparison-icon" set:html={iconMap.shield} />
            <h4>Rentabilidade Garantida</h4>
          </div>
          <ul class="comparison-list positive">
            <li>
              <div class="list-icon" set:html={iconMap.check} />
              Valor mensal fixo definido em contrato
            </li>
            <li>
              <div class="list-icon" set:html={iconMap.check} />
              Receita independente da ocupação
            </li>
            <li>
              <div class="list-icon" set:html={iconMap.check} />
              Gestão operacional e manutenção incluídas conforme contrato
            </li>
            <li>
              <div class="list-icon" set:html={iconMap.check} />
              Previsibilidade financeira total
            </li>
            <li>
              <div class="list-icon" set:html={iconMap.check} />
              Zero envolvimento na operação diária
            </li>
          </ul>
        </div>
      </div>
    </div>

    <!-- Ideal For -->
    <div class="ideal-for">
      <h3>Este Modelo é Ideal Para Quem...</h3>
      <div class="ideal-grid">
        <div class="ideal-card">
          <div class="ideal-icon" set:html={iconMap.heart} />
          <h4>Procura Tranquilidade Total</h4>
          <p>Não quer lidar com reservas, hóspedes, avarias, limpeza ou imprevistos do dia a dia. Prefere delegar toda a operação e receber o rendimento de forma automática.</p>
        </div>

        <div class="ideal-card">
          <div class="ideal-icon" set:html={iconMap.chartBar} />
          <h4>Precisa de Receita Previsível</h4>
          <p>Valoriza saber exatamente quanto vai receber todos os meses para planear despesas, poupança ou outros investimentos, sem depender da sazonalidade.</p>
        </div>

        <div class="ideal-card">
          <div class="ideal-icon" set:html={iconMap.checkCircle} />
          <h4>Prefere Estabilidade a Risco</h4>
          <p>Aceita um rendimento estável e contratualizado em vez de arriscar variações mensais, períodos de vacância ou custos inesperados.</p>
        </div>

        <div class="ideal-card">
          <div class="ideal-icon" set:html={iconMap.clock} />
          <h4>Não Está Disponível Para Gerir</h4>
          <p>Vive fora, viaja com frequência ou simplesmente não tem tempo (nem interesse) em acompanhar a gestão diária do imóvel.</p>
        </div>
      </div>
    </div>

    <!-- CTA Final -->
    <div class="final-cta">
      <div class="cta-content">
        <h3>Quer Saber se a Rentabilidade Garantida é Indicada Para o Seu Imóvel?</h3>
        <p>Solicite uma avaliação gratuita e descubra o valor mensal que pode receber com base nas características reais do seu imóvel.</p>
        <div class="cta-buttons">
          <a href="https://hostwise.typeform.com/to/pWgP4EuZ?" class="btn-primary-large">
            Solicitar Avaliação Gratuita
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .guaranteed-rent {
    padding: 100px 20px;
    background: linear-gradient(180deg, white 0%, var(--blue-light) 50%, white 100%);
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
  }

  /* Header */
  .section-header {
    text-align: center;
    max-width: 800px;
    margin: 0 auto 80px;
  }

  .header-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
    color: white;
    padding: 10px 24px;
    border-radius: 30px;
    font-weight: 600;
    font-size: 0.9rem;
    margin-bottom: 24px;
    box-shadow: 0 4px 16px rgba(12, 68, 99, 0.2);
  }

  .header-badge .badge-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 20px;
    height: 20px;
  }

  .header-badge .badge-icon svg {
    width: 20px;
    height: 20px;
  }

  .section-header h2 {
    font-size: 3rem;
    color: var(--primary);
    margin-bottom: 20px;
    line-height: 1.2;
  }

  .section-header p {
    font-size: 1.15rem;
    color: var(--text);
    line-height: 1.7;
    opacity: 0.9;
  }

  /* Hero Visual */
  .hero-visual {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 32px;
    margin-bottom: 100px;
  }

  .main-card {
    background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
    border-radius: 24px;
    padding: 48px;
    color: white;
    box-shadow: 0 20px 60px rgba(12, 68, 99, 0.3);
    position: relative;
    overflow: hidden;
  }

  .main-card::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, transparent 70%);
    animation: pulse 8s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { transform: scale(1); opacity: 0.5; }
    50% { transform: scale(1.1); opacity: 0.3; }
  }

  .card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 32px;
    position: relative;
    z-index: 2;
  }

  .card-label {
    font-size: 0.95rem;
    opacity: 0.9;
    font-weight: 500;
  }

  .guaranteed-badge {
    display: flex;
    align-items: center;
    gap: 6px;
    background: rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(10px);
    padding: 6px 16px;
    border-radius: 20px;
    font-size: 0.85rem;
    font-weight: 600;
  }

  .guaranteed-badge .badge-shield-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 16px;
    height: 16px;
  }

  .guaranteed-badge .badge-shield-icon svg {
    width: 16px;
    height: 16px;
  }

  .amount-display {
    display: flex;
    align-items: baseline;
    margin-bottom: 40px;
    position: relative;
    z-index: 2;
  }

  .currency {
    font-size: 3rem;
    font-weight: 600;
    margin-right: 8px;
  }

  .amount {
    font-size: 5rem;
    font-weight: 700;
    line-height: 1;
    transition: opacity 0.3s ease, transform 0.3s ease;
  }

  .period {
    font-size: 1.5rem;
    opacity: 0.9;
    margin-left: 12px;
  }

  .payment-info {
    display: flex;
    flex-direction: column;
    gap: 16px;
    position: relative;
    z-index: 2;
  }

  .info-item {
    display: flex;
    align-items: center;
    gap: 12px;
    font-size: 1.05rem;
    font-weight: 500;
  }

  .info-item .info-icon {
    flex-shrink: 0;
    background: rgba(255, 255, 255, 0.2);
    padding: 4px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
  }

  .info-item .info-icon svg {
    width: 20px;
    height: 20px;
  }

  /* Visual Stats */
  .visual-stats {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .stat-card {
    background: white;
    border-radius: 16px;
    padding: 24px;
    display: flex;
    align-items: center;
    gap: 20px;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
    transition: all 0.3s;
  }

  .stat-card:hover {
    transform: translateX(8px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
  }

  .stat-icon {
    width: 56px;
    height: 56px;
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    padding: 12px;
  }

  .stat-icon svg {
    width: 24px;
    height: 24px;
  }

  .stat-icon.green { background: linear-gradient(135deg, #10b981 0%, #059669 100%); color: white; }
  .stat-icon.blue { background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%); color: white; }
  .stat-icon.orange { background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%); color: white; }

  .stat-content {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .stat-value {
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--primary);
    line-height: 1;
  }

  .stat-label {
    font-size: 0.9rem;
    color: var(--text);
    line-height: 1.3;
  }

  /* How it Works */
  .how-it-works {
    margin-bottom: 100px;
  }

  .how-it-works h3 {
    text-align: center;
    font-size: 2.25rem;
    color: var(--primary);
    margin-bottom: 60px;
  }

  .steps-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
  }

  .step-card {
    background: white;
    border: 2px solid var(--gray-light);
    border-radius: 20px;
    padding: 32px 24px;
    text-align: center;
    position: relative;
    transition: all 0.3s;
  }

  .step-card:hover {
    border-color: var(--secondary);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
    transform: translateY(-8px);
  }

  .step-number {
    width: 56px;
    height: 56px;
    background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    font-weight: 700;
    margin: 0 auto 20px;
  }

  .step-content h4 {
    font-size: 1.15rem;
    color: var(--primary);
    margin-bottom: 12px;
  }

  .step-content p {
    font-size: 0.95rem;
    color: var(--text);
    line-height: 1.6;
    margin: 0;
  }

  .step-arrow {
    position: absolute;
    top: 50%;
    right: -28px;
    transform: translateY(-50%);
    color: var(--secondary);
    opacity: 0.5;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .step-arrow svg {
    width: 32px;
    height: 32px;
  }

  .step-card:last-child .step-arrow {
    display: none;
  }

  /* Comparison */
  .comparison-section {
    margin-bottom: 100px;
  }

  .comparison-section h3 {
    text-align: center;
    font-size: 2.25rem;
    color: var(--primary);
    margin-bottom: 60px;
  }

  .comparison-grid {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    gap: 40px;
    align-items: center;
  }

  .comparison-card {
    background: white;
    border-radius: 20px;
    padding: 40px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.08);
  }

  .comparison-header {
    display: flex;
    align-items: center;
    gap: 16px;
    padding-bottom: 24px;
    border-bottom: 2px solid var(--gray-light);
    margin-bottom: 24px;
  }

  .comparison-header .comparison-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
  }

  .comparison-header .comparison-icon svg {
    width: 28px;
    height: 28px;
  }

  .comparison-header.traditional {
    color: #ef4444;
  }

  .comparison-header.guaranteed {
    color: #10b981;
  }

  .comparison-header h4 {
    font-size: 1.5rem;
    margin: 0;
  }

  .comparison-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .comparison-list li {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    font-size: 1rem;
    line-height: 1.5;
  }

  .comparison-list .list-icon {
    flex-shrink: 0;
    margin-top: 2px;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 20px;
    height: 20px;
  }

  .comparison-list .list-icon svg {
    width: 20px;
    height: 20px;
  }

  .comparison-list.negative li {
    color: #666;
  }

  .comparison-list.negative .list-icon {
    color: #ef4444;
  }

  .comparison-list.positive .list-icon {
    color: #10b981;
  }

  .comparison-divider {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .comparison-divider span {
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 1.25rem;
    box-shadow: 0 4px 16px rgba(12, 68, 99, 0.3);
  }

  /* Ideal For */
  .ideal-for {
    margin-bottom: 80px;
  }

  .ideal-for h3 {
    text-align: center;
    font-size: 2.25rem;
    color: var(--primary);
    margin-bottom: 60px;
  }

  .ideal-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
  }

  .ideal-card {
    background: white;
    border: 2px solid var(--gray-light);
    border-radius: 20px;
    padding: 32px 24px;
    text-align: center;
    transition: all 0.3s;
  }

  .ideal-card:hover {
    border-color: var(--secondary);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
    transform: translateY(-4px);
  }

  .ideal-icon {
    width: 72px;
    height: 72px;
    background: linear-gradient(135deg, var(--blue-light) 0%, rgba(92, 176, 214, 0.2) 100%);
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 20px;
    color: var(--primary);
    padding: 16px;
  }

  .ideal-icon svg {
    width: 32px;
    height: 32px;
  }

  .ideal-card h4 {
    font-size: 1.15rem;
    color: var(--primary);
    margin-bottom: 12px;
  }

  .ideal-card p {
    font-size: 0.95rem;
    color: var(--text);
    line-height: 1.6;
    margin: 0;
  }

  /* Final CTA */
  .final-cta {
    background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
    border-radius: 24px;
    padding: 60px 48px;
    text-align: center;
    color: white;
    box-shadow: 0 20px 60px rgba(12, 68, 99, 0.3);
  }

  .cta-content h3 {
    font-size: 2.25rem;
    margin-bottom: 16px;
  }

  .cta-content p {
    font-size: 1.15rem;
    margin-bottom: 32px;
    opacity: 0.95;
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
  }

  .cta-buttons {
    display: flex;
    gap: 16px;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn-primary-large {
    padding: 18px 36px;
    background: white;
    color: var(--primary);
    border-radius: 12px;
    font-size: 1.1rem;
    font-weight: 600;
    transition: all 0.3s;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.1);
  }

  .btn-primary-large:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
  }

  .btn-secondary-outline {
    padding: 18px 36px;
    background: transparent;
    color: white;
    border: 2px solid white;
    border-radius: 12px;
    font-size: 1.1rem;
    font-weight: 600;
    transition: all 0.3s;
  }

  .btn-secondary-outline:hover {
    background: white;
    color: var(--primary);
    transform: translateY(-2px);
  }

  /* Responsive */
  @media (max-width: 1200px) {
    .steps-grid,
    .ideal-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .step-arrow {
      display: none;
    }
  }

  @media (max-width: 1024px) {
    .hero-visual {
      grid-template-columns: 1fr;
    }

    .comparison-grid {
      grid-template-columns: 1fr;
    }

    .comparison-divider {
      transform: rotate(90deg);
    }

    .section-header h2 {
      font-size: 2.25rem;
    }

    .how-it-works h3,
    .comparison-section h3,
    .ideal-for h3 {
      font-size: 2rem;
    }
  }

  @media (max-width: 768px) {
    .guaranteed-rent {
      padding: 80px 20px;
    }

    .section-header {
      margin-bottom: 60px;
    }

    .section-header h2 {
      font-size: 2rem;
    }

    .main-card {
      padding: 36px 28px;
    }

    .amount {
      font-size: 3.5rem;
    }

    .steps-grid,
    .ideal-grid {
      grid-template-columns: 1fr;
    }

    .final-cta {
      padding: 48px 28px;
    }

    .cta-content h3 {
      font-size: 1.75rem;
    }

    .cta-buttons {
      flex-direction: column;
    }

    .btn-primary-large,
    .btn-secondary-outline {
      width: 100%;
    }
  }
</style>

<script>
  class AmountAnimator {
    private amounts: string[] = ['800', '1.200', '1.500', '2.000', '2.500'];
    private currentIndex: number = 0;
    private intervalId: number | null = null;
    private element: HTMLElement | null = null;

    constructor() {
      this.element = document.getElementById('animatedAmount');
      if (this.element) {
        this.startAnimation();
      }
    }

    startAnimation() {
      if (!this.element) return;

      this.intervalId = window.setInterval(() => {
        this.currentIndex = (this.currentIndex + 1) % this.amounts.length;
        if (this.element) {
          this.element.style.opacity = '0';
          this.element.style.transform = 'translateY(-10px)';

          setTimeout(() => {
            if (this.element) {
              this.element.textContent = this.amounts[this.currentIndex];
              this.element.style.opacity = '1';
              this.element.style.transform = 'translateY(0)';
            }
          }, 300);
        }
      }, 2000);
    }

    destroy() {
      if (this.intervalId) {
        clearInterval(this.intervalId);
      }
    }
  }

  if (typeof window !== 'undefined') {
    let animator: AmountAnimator | null = null;

    document.addEventListener('DOMContentLoaded', () => {
      animator = new AmountAnimator();
    });

    // Cleanup on page unload
    window.addEventListener('beforeunload', () => {
      if (animator) {
        animator.destroy();
      }
    });
  }
</script>