---
import OptimizedImage from './OptimizedImage.astro';
import type { ImageMetadata } from 'astro';
import { property1, property2 } from '../lib/fallback-images';

interface Benefit {
  label?: string;
}

interface WPImage {
  url?: string;
  alt?: string;
  source_url?: string;
}

interface ToggleSectionProps {
  ownerTitle?: string;
  ownerDescription?: string;
  ownerBenefits?: Benefit[];
  ownerImage?: WPImage | ImageMetadata | string;
  ownerCtaLabel?: string;
  ownerCtaUrl?: string;

  guestTitle?: string;
  guestDescription?: string;
  guestBenefits?: Benefit[];
  guestImage?: WPImage | ImageMetadata | string;
  guestCtaLabel?: string;
  guestCtaUrl?: string;
}

// Normaliza imagem vinda do ACF (string, objeto ou undefined)
function normalizeImage(
  img: WPImage | ImageMetadata | string | undefined,
  fallback: ImageMetadata,
  fallbackAlt: string
): { src: ImageMetadata | string; alt: string } {
  if (!img) {
    return { src: fallback, alt: fallbackAlt };
  }

  // ImageMetadata (imported local image)
  if (typeof img === 'object' && 'src' in img && 'width' in img) {
    return { src: img as ImageMetadata, alt: fallbackAlt };
  }

  if (typeof img === 'string') {
    return {
      src: img || fallback,
      alt: fallbackAlt,
    };
  }

  // WPImage
  return {
    src: img.url || img.source_url || fallback,
    alt: img.alt || fallbackAlt,
  };
}

const {
  ownerTitle = 'Para Proprietários e Investidores',
  ownerDescription = 'Gestão profissional end-to-end: operação diária, otimização de receitas, e estratégia de valorização. Do apartamento individual ao edifício completo — oferecemos o modelo de gestão adequado à escala do seu investimento.',
  ownerBenefits = [],
  ownerImage,
  ownerCtaLabel = 'Saber Mais',
  ownerCtaUrl = '#contacto',

  guestTitle = 'Para Hóspedes',
  guestDescription = 'Reserve diretamente connosco e aproveite as melhores tarifas numa plataforma segura e confiável.',
  guestBenefits = [],
  guestImage,
  guestCtaLabel = 'Ver Alojamentos',
  guestCtaUrl = 'https://hostwisebooking.pt',
} = Astro.props as ToggleSectionProps;

const defaultOwnerBenefits: Benefit[] = ownerBenefits.length
  ? ownerBenefits
  : [
      { label: 'Gestão de Alojamento Local (AL)' },
      { label: 'Gestão de Prédios Inteiros Convertidos em AL' },
      { label: 'Asset Management de Hospitalidade' },
    ];

const defaultGuestBenefits: Benefit[] = guestBenefits.length
  ? guestBenefits
  : [
      { label: 'Melhores preços garantidos sem taxas de plataforma' },
      { label: 'Alojamentos selecionados e verificados pela nossa equipa' },
      { label: 'Reservas seguras e suporte dedicado 24/7' },
      { label: 'Check-in simplificado e experiência premium' },
    ];

const ownerImg = normalizeImage(
  ownerImage,
  property1,
  'Gestão de propriedades Host Wise'
);

const guestImg = normalizeImage(
  guestImage,
  property2,
  'Reservas diretas Host Wise'
);
---

<section class="toggle-section">
  <div class="toggle-container">

    <!-- CARD PROPRIETÁRIOS -->
    <div class="toggle-card" data-section="owners" data-expanded="true">
      <div class="card-content">
        <div class="card-header">
          <h3>{ownerTitle}</h3>
          <p>{ownerDescription}</p>
        </div>

        <ul class="benefits-list">
          {defaultOwnerBenefits.map((item) => (
            <li>
              <div class="icon-check">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41L9 16.17z" />
                </svg>
              </div>
              <span>{item.label}</span>
            </li>
          ))}
        </ul>

        <div class="card-image">
          <OptimizedImage src={ownerImg.src} alt={ownerImg.alt} loading="lazy" />
        </div>

        <div class="card-cta">
          <a href={ownerCtaUrl} class="btn-primary">
            {ownerCtaLabel}
          </a>
        </div>
      </div>
    </div>

    <!-- CARD HÓSPEDES -->
    <div class="toggle-card" data-section="guests" data-expanded="false">
      <div class="card-content">
        <div class="card-header">
          <h3>{guestTitle}</h3>
          <p>{guestDescription}</p>
        </div>

        <ul class="benefits-list">
          {defaultGuestBenefits.map((item) => (
            <li>
              <div class="icon-check">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41L9 16.17z" />
                </svg>
              </div>
              <span>{item.label}</span>
            </li>
          ))}
        </ul>

        <div class="card-image">
          <OptimizedImage src={guestImg.src} alt={guestImg.alt} loading="lazy" />
        </div>

        <div class="card-cta">
          <a href={guestCtaUrl} target="_blank" rel="noopener noreferrer" class="btn-primary">
            {guestCtaLabel}
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" width="20" height="20">
              <path d="M10 6L8.59 7.41 13.17 12l-4.58 4.59L10 18l6-6-6-6z" />
            </svg>
          </a>
        </div>
      </div>
    </div>

  </div>
</section>


<style>
  .toggle-section {
    padding: 0;
    background: white;
  }

  .toggle-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 80px 20px;
    display: flex;
    gap: 20px;
    align-items: stretch;
  }

  .toggle-card {
    position: relative;
    flex: 1;
    border-radius: 16px;
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    cursor: pointer;
    background: linear-gradient(180deg, rgba(12, 68, 99, 0.95) 0%, rgba(12, 68, 99, 0.85) 100%);
  }

  .toggle-card::before {
    content: '';
    position: absolute;
    inset: 0;
    z-index: 1;
    background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
    opacity: 0.8;
    transition: opacity 0.4s ease;
  }

  .toggle-card[data-section="guests"]::before {
    background: linear-gradient(135deg, var(--secondary) 0%, var(--blue) 100%);
  }

  .toggle-card[data-expanded="true"] {
    flex: 1.8;
  }

  .toggle-card[data-expanded="true"]::before {
    opacity: 0.9;
  }

  .card-content {
    position: relative;
    z-index: 2;
    padding: 48px;
    height: 100%;
    display: flex;
    flex-direction: column;
    gap: 32px;
    color: white;
  }

  .card-header h3 {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 12px;
    color: white;
  }

  .card-header p {
    font-size: 1.15rem;
    line-height: 1.6;
    opacity: 0.95;
    max-width: 500px;
  }

  .benefits-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 16px;
    opacity: 0;
    max-height: 0;
    overflow: hidden;
    transition: all 0.4s ease;
  }

  .toggle-card[data-expanded="true"] .benefits-list {
    opacity: 1;
    max-height: 500px;
    transition-delay: 0.2s;
  }

  .benefits-list li {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    font-size: 1.05rem;
    line-height: 1.5;
  }

  .icon-check {
    width: 24px;
    height: 24px;
    min-width: 24px;
    background: rgba(255, 255, 255, 0.2);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    backdrop-filter: blur(10px);
    border: 2px solid rgba(255, 255, 255, 0.3);
  }

  .icon-check svg {
    width: 14px;
    height: 14px;
  }

  .card-image {
  opacity: 0;
  max-height: 0;
  overflow: hidden;
  transition: all 0.4s ease;
  border-radius: 12px;
}

.toggle-card[data-expanded="true"] .card-image {
  opacity: 1;
  max-height: 300px;
  transition-delay: 0.3s;
}

/* picture ocupa tudo */
.card-image :global(picture) {
  display: block;
  width: 100%;
  height: 250px;
}

/* img dentro do picture */
.card-image :global(img) {
  display: block;
  width: 100%;
  height: 100%;
  object-fit: cover;

  /* 👇 isto garante centro vertical */
  object-position: center center;

  border-radius: 12px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
}

  .card-cta {
    margin-top: auto;
  }

  .card-cta .btn-primary {
    background: white;
    color: var(--primary);
    font-weight: 600;
    display: inline-flex;
    align-items: center;
    gap: 8px;
    transition: all 0.3s ease;
  }

  .card-cta .btn-primary:hover {
    background: var(--blue-light);
    color: var(--primary);
    gap: 12px;
  }

  .toggle-card[data-section="guests"] .btn-primary {
    color: var(--secondary);
  }

  /* Hover states */
  .toggle-card:not([data-expanded="true"]):hover {
    transform: scale(1.02);
  }

  .toggle-card:not([data-expanded="true"]):hover::before {
    opacity: 0.85;
  }

  /* Mobile */
  @media (max-width: 968px) {
    .toggle-container {
      flex-direction: column;
      padding: 60px 20px;
    }

    .toggle-card {
      min-height: 400px;
    }

    .toggle-card[data-expanded="true"],
    .toggle-card[data-expanded="false"] {
      flex: 1;
    }

    .card-content {
      padding: 32px;
    }

    .card-header h3 {
      font-size: 1.75rem;
    }

    .benefits-list,
    .card-image {
      opacity: 1;
      max-height: none;
    }

    .toggle-card[data-expanded="true"] .card-image {
      max-height: none;
    }

    .card-image img {
      height: 200px;
    }
  }
</style>

<script>
  class ToggleCards {
    private cards: NodeListOf<HTMLElement>;

    constructor() {
      this.cards = document.querySelectorAll('.toggle-card');
      this.init();
    }

    init() {
      // Only enable hover toggle on desktop
      if (window.innerWidth > 968) {
        this.cards.forEach(card => {
          card.addEventListener('mouseenter', () => this.expandCard(card));
        });
      }
    }

    expandCard(hoveredCard: HTMLElement) {
      // Collapse all
      this.cards.forEach(card => {
        card.setAttribute('data-expanded', 'false');
      });

      // Expand hovered
      hoveredCard.setAttribute('data-expanded', 'true');
    }
  }

  // Initialize
  if (typeof window !== 'undefined') {
    document.addEventListener('DOMContentLoaded', () => {
      new ToggleCards();
    });
  }
</script>