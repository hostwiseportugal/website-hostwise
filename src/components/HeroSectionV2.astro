---
// Hero Section V2 - SaaS-style centered layout
---

<section class="hero-v2">
  <div class="hero-v2-container">
    <div class="hero-v2-content">
      <!-- Badge -->
      <div class="hero-v2-badge">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" width="16" height="16">
          <path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4z"/>
        </svg>
        Rentabilidade Garantida
      </div>

      <!-- Heading -->
      <h1 class="hero-v2-heading">
        Receba uma Renda Fixa Todos os Meses, Sem Surpresas
      </h1>

      <!-- Description -->
      <p class="hero-v2-description">
        Esqueça a incerteza do alojamento local. Com o modelo de Rentabilidade Garantida,
        recebe o mesmo valor todos os meses, independentemente da ocupação. Segurança financeira
        para planear o seu futuro com tranquilidade.
      </p>

      <!-- Feature Points -->
      <div class="hero-v2-features">
        <div class="hero-v2-feature">
          <div class="feature-v2-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" width="20" height="20">
              <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41L9 16.17z"/>
            </svg>
          </div>
          <span>Renda Mensal Fixa</span>
        </div>
        <div class="hero-v2-feature">
          <div class="feature-v2-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" width="20" height="20">
              <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41L9 16.17z"/>
            </svg>
          </div>
          <span>100% de Ocupação Garantida</span>
        </div>
        <div class="hero-v2-feature">
          <div class="feature-v2-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" width="20" height="20">
              <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41L9 16.17z"/>
            </svg>
          </div>
          <span>Manutenção Incluída</span>
        </div>
      </div>

      <!-- CTAs -->
      <div class="hero-v2-cta-group">
        <a href="#contacto" class="btn-v2-primary">
          Receber Proposta Gratuita
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" width="20" height="20">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
          </svg>
        </a>
        <a href="#beneficios" class="btn-v2-secondary">
          Saber Mais
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" width="20" height="20">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
          </svg>
        </a>
      </div>
    </div>

    <!-- Featured Image -->
    <div class="hero-v2-visual">
      <div class="hero-v2-image-wrapper">
        <img
          src="/images/property-1.webp"
          alt="Propriedade em Alojamento Local"
          class="hero-v2-image"
        />

        <!-- Stats Overlay Cards -->
        <div class="hero-v2-stats">
          <div class="stat-v2-card stat-v2-left">
            <div class="stat-v2-icon">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" width="24" height="24">
                <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 18c-4.41 0-8-3.59-8-8s3.59-8 8-8 8 3.59 8 8-3.59 8-8 8zm.31-8.86c-1.77-.45-2.34-.94-2.34-1.67 0-.84.79-1.43 2.1-1.43 1.38 0 1.9.66 1.94 1.64h1.71c-.05-1.34-.87-2.57-2.49-2.97V5H10.9v1.69c-1.51.32-2.72 1.3-2.72 2.81 0 1.79 1.49 2.69 3.66 3.21 1.95.46 2.34 1.15 2.34 1.87 0 .53-.39 1.39-2.1 1.39-1.6 0-2.23-.72-2.32-1.64H8.04c.1 1.7 1.36 2.66 2.86 2.97V19h2.34v-1.67c1.52-.29 2.72-1.16 2.73-2.77-.01-2.2-1.9-2.96-3.66-3.42z"/>
              </svg>
            </div>
            <div class="stat-v2-content">
              <div class="stat-v2-label">Renda Mensal</div>
              <div class="stat-v2-value">€1.200 - €2.500</div>
            </div>
          </div>

          <div class="stat-v2-card stat-v2-right">
            <div class="stat-v2-icon">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" width="24" height="24">
                <path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4z"/>
              </svg>
            </div>
            <div class="stat-v2-content">
              <div class="stat-v2-label">Garantia</div>
              <div class="stat-v2-value">100%</div>
            </div>
          </div>
        </div>

        <!-- Decorative Elements -->
        <div class="hero-v2-gradient"></div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero-v2 {
    padding: 180px 20px 120px;
    background: linear-gradient(180deg, #F8FCFF 0%, #fff 100%);
    position: relative;
    overflow: hidden;
  }

  /* Background decoration */
  .hero-v2::before {
    content: '';
    position: absolute;
    top: -200px;
    left: 50%;
    transform: translateX(-50%);
    width: 1200px;
    height: 1200px;
    background: radial-gradient(circle, rgba(92, 176, 214, 0.06) 0%, transparent 70%);
    pointer-events: none;
  }

  .hero-v2-container {
    max-width: 1200px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  /* Content - Centered */
  .hero-v2-content {
    text-align: center;
    margin-bottom: 80px;
    animation: fadeInUp 0.8s ease-out;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Badge */
  .hero-v2-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: linear-gradient(135deg, rgba(92, 176, 214, 0.15) 0%, rgba(92, 176, 214, 0.08) 100%);
    color: var(--secondary);
    padding: 10px 24px;
    border-radius: 30px;
    font-weight: 700;
    font-size: 0.9rem;
    margin-bottom: 32px;
    border: 1px solid rgba(92, 176, 214, 0.2);
    animation: fadeInDown 0.8s ease-out 0.2s backwards;
  }

  @keyframes fadeInDown {
    from {
      opacity: 0;
      transform: translateY(-20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .hero-v2-badge svg {
    flex-shrink: 0;
  }

  /* Heading */
  .hero-v2-heading {
    font-size: 4rem;
    font-weight: 800;
    color: var(--primary);
    line-height: 1.15;
    margin-bottom: 24px;
    letter-spacing: -0.02em;
    max-width: 900px;
    margin-left: auto;
    margin-right: auto;
    animation: fadeInUp 0.8s ease-out 0.3s backwards;
  }

  /* Description */
  .hero-v2-description {
    font-size: 1.3rem;
    line-height: 1.7;
    color: var(--text);
    opacity: 0.85;
    margin-bottom: 40px;
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
    animation: fadeInUp 0.8s ease-out 0.4s backwards;
  }

  /* Features */
  .hero-v2-features {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 32px;
    margin-bottom: 48px;
    flex-wrap: wrap;
    animation: fadeInUp 0.8s ease-out 0.5s backwards;
  }

  .hero-v2-feature {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .feature-v2-icon {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--secondary) 0%, rgba(92, 176, 214, 1) 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    flex-shrink: 0;
  }

  .hero-v2-feature span {
    font-size: 1.05rem;
    font-weight: 600;
    color: var(--primary);
  }

  /* CTA Group */
  .hero-v2-cta-group {
    display: flex;
    justify-content: center;
    gap: 16px;
    flex-wrap: wrap;
    animation: fadeInUp 0.8s ease-out 0.6s backwards;
  }

  .btn-v2-primary,
  .btn-v2-secondary {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    padding: 18px 36px;
    border-radius: 12px;
    font-size: 1.1rem;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .btn-v2-primary {
    background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
    color: white;
    box-shadow: 0 4px 16px rgba(12, 68, 99, 0.25);
  }

  .btn-v2-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 28px rgba(12, 68, 99, 0.35);
  }

  .btn-v2-secondary {
    background: white;
    color: var(--primary);
    border: 2px solid var(--primary);
    box-shadow: 0 2px 8px rgba(12, 68, 99, 0.1);
  }

  .btn-v2-secondary:hover {
    background: var(--blue-light);
    transform: translateY(-2px);
    box-shadow: 0 4px 16px rgba(12, 68, 99, 0.15);
  }

  /* Visual Section */
  .hero-v2-visual {
    max-width: 1000px;
    margin: 0 auto;
    animation: fadeInScale 1s ease-out 0.7s backwards;
  }

  @keyframes fadeInScale {
    from {
      opacity: 0;
      transform: scale(0.95);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  .hero-v2-image-wrapper {
    position: relative;
    border-radius: 24px;
    overflow: hidden;
    box-shadow: 0 24px 80px rgba(12, 68, 99, 0.2);
    transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .hero-v2-image-wrapper:hover {
    transform: translateY(-8px);
  }

  .hero-v2-image {
    width: 100%;
    height: auto;
    display: block;
    object-fit: cover;
    aspect-ratio: 16/10;
  }

  .hero-v2-gradient {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(12, 68, 99, 0.4) 0%, transparent 50%);
    pointer-events: none;
  }

  /* Stats Cards */
  .hero-v2-stats {
    position: absolute;
    bottom: 24px;
    left: 24px;
    right: 24px;
    display: flex;
    gap: 16px;
    z-index: 10;
  }

  .stat-v2-card {
    flex: 1;
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    border-radius: 16px;
    padding: 20px;
    display: flex;
    gap: 16px;
    align-items: center;
    box-shadow: 0 8px 32px rgba(12, 68, 99, 0.15);
    border: 1px solid rgba(255, 255, 255, 0.5);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    opacity: 0;
    animation: slideInUp 0.6s ease-out forwards;
  }

  .stat-v2-left {
    animation-delay: 0.9s;
  }

  .stat-v2-right {
    animation-delay: 1s;
  }

  @keyframes slideInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .stat-v2-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 40px rgba(12, 68, 99, 0.25);
  }

  .stat-v2-icon {
    width: 48px;
    height: 48px;
    border-radius: 12px;
    background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    flex-shrink: 0;
  }

  .stat-v2-content {
    flex: 1;
  }

  .stat-v2-label {
    font-size: 0.85rem;
    color: var(--text);
    opacity: 0.7;
    margin-bottom: 4px;
    font-weight: 500;
  }

  .stat-v2-value {
    font-size: 1.4rem;
    font-weight: 700;
    color: var(--primary);
    line-height: 1;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .hero-v2 {
      padding: 160px 20px 100px;
    }

    .hero-v2-heading {
      font-size: 3rem;
    }

    .hero-v2-description {
      font-size: 1.15rem;
    }

    .hero-v2-content {
      margin-bottom: 60px;
    }
  }

  @media (max-width: 768px) {
    .hero-v2 {
      padding: 140px 20px 80px;
    }

    .hero-v2-heading {
      font-size: 2.5rem;
    }

    .hero-v2-description {
      font-size: 1.1rem;
    }

    .hero-v2-features {
      flex-direction: column;
      gap: 16px;
    }

    .hero-v2-cta-group {
      flex-direction: column;
      width: 100%;
    }

    .btn-v2-primary,
    .btn-v2-secondary {
      width: 100%;
      justify-content: center;
    }

    .hero-v2-stats {
      flex-direction: column;
    }

    .stat-v2-card {
      width: 100%;
    }
  }

  @media (max-width: 480px) {
    .hero-v2 {
      padding: 120px 20px 60px;
    }

    .hero-v2-heading {
      font-size: 2rem;
    }

    .hero-v2-description {
      font-size: 1rem;
    }

    .hero-v2-badge {
      font-size: 0.85rem;
      padding: 8px 20px;
    }

    .hero-v2-feature span {
      font-size: 0.95rem;
    }

    .feature-v2-icon {
      width: 36px;
      height: 36px;
    }

    .hero-v2-stats {
      bottom: 16px;
      left: 16px;
      right: 16px;
    }

    .stat-v2-card {
      padding: 16px;
    }

    .stat-v2-icon {
      width: 40px;
      height: 40px;
    }

    .stat-v2-value {
      font-size: 1.2rem;
    }
  }
</style>
