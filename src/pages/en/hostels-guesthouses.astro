---
import LayoutAd from '../../layouts/LayoutAd.astro';
import hostelHeader from '../../assets/images/hostel-guesthouse-header-desktop.webp';
import HeroSectionV3 from '../../components/HeroSectionV3.astro';
import FAQSection from '../../components/FAQSection.astro';
import { iconMap } from '../../icons/iconMap';
import { serviceSchema } from "../../lib/schema/service";
import { faqPageSchemaFromCategories } from "../../lib/schema/faq-from-categories";
import { breadcrumbSchema } from "../../lib/schema/breadcrumbs";
import { Image } from 'astro:assets';
import hostelArvore from '../../assets/images/hostels-host-wise-arvore.webp';
import portoLoungeRefeitorio from '../../assets/images/porto-lounge-hostel-refeitorio.webp';
import hostelPortoLounge from '../../assets/images/hostels-porto-lounge-hostel.webp';
import arvoreVistaAerea from '../../assets/images/host-wise-arvore-vista-aerea.webp';
import hostelBurroVille from '../../assets/images/hostels-burro-ville.webp';

const site = Astro.site ?? new URL("https://www.hostwise.pt");
const canonicalUrl = new URL(Astro.url); canonicalUrl.search = "";
const canonical = canonicalUrl.href;

const metaDescription = "Fixed monthly rent for your hostel or guesthouse. Full management of your accommodation in exchange for predictable income, every month, with Host Wise.";

const faqCategories = [
  {
    id: 'model',
    label: 'About the Model',
    questions: [
      {
        question: 'Why choose Host Wise?',
        answer: '<p>Host Wise has 8 years of experience in vacation rental management in Portugal. Our average occupancy rate is 80%, with an average profitability increase of 40% for the property owners who work with us.</p>',
      },
      {
        question: 'How do you guarantee the full rent amount?',
        answer: '<p>The rent amount is protected by a bank guarantee, ensuring full payment of the agreed rent throughout the duration of the contract.</p>',
      },
      {
        question: 'How do you determine my property\'s value?',
        answer: '<p>The valuation is done through a profitability study that considers the location, size and condition of the property, and current real estate market trends. This study allows us to predict the net revenue generated for the proposed contract term.</p>',
      },
    ],
  },
  {
    id: 'contract',
    label: 'Contract & Payment',
    questions: [
      {
        question: 'What is the contract duration?',
        answer: '<p>Contracts with Host Wise have variable durations according to your preferences, and can last up to 10 years.</p>',
      },
      {
        question: 'What are the payment terms?',
        answer: '<p>You will receive the money in your account automatically every month by the 8th.</p>',
      },
      {
        question: 'What is the next step?',
        answer: '<p>The process is simple: first, submit your accommodation details for a free evaluation. Then, you will receive a fair offer based on the current market. If you accept the offer, we will proceed with the legal formalities to complete the lease.</p>',
      },
    ],
  },
];

const schemas = [
  breadcrumbSchema([
    { name: "Home", url: site.href },
    { name: "Hostels & Guesthouses", url: canonical },
  ]),
  serviceSchema({
    serviceName: "Hostel and Guesthouse Management",
    url: canonical,
    providerUrl: "https://www.hostwise.pt",
    areaServed: [{ name: "Portugal", type: "Country" }],
  }),
  ...(faqCategories.length ? [faqPageSchemaFromCategories(faqCategories)] : []),
];

const alternates = [
  { hreflang: "pt-PT", href: new URL("/hostels-guesthouses", site).href },
  { hreflang: "en", href: canonical },
  { hreflang: "x-default", href: new URL("/hostels-guesthouses", site).href },
];
---

<LayoutAd
  title="Hostels & Guesthouses — Fixed Monthly Rent"
  description={metaDescription}
  schemas={schemas}
  alternates={alternates}
  canonical={canonical}
>
  <!-- Hero Section -->
  <HeroSectionV3
    badge_text="Hostels & Guesthouses"
    badge_icon_key="hotel"
    heading="Fixed Monthly Rent for Your Hostel and/or Guesthouse"
    description="Full management of your accommodation in exchange for predictable income, every month."
    primary_cta_label="Get a Proposal"
    primary_cta_url="#form"
    secondary_cta_label="Learn More"
    secondary_cta_url="#challenge"
    image="/images/hostel-guesthouse-header-desktop.webp"
  />

  <!-- Typeform Embed (after hero) -->
  <section class="typeform-section" id="form">
    <div class="container">
      <div class="typeform-header">
        <span class="section-badge">Free Proposal</span>
        <h2>Get a Valuation of Your Accommodation</h2>
        <p>Fill in the form and our team will get in touch with you shortly.</p>
      </div>
      <div class="form-container">
        <div data-tf-live="01J881SMFDDP5K5A65ZF2F7XEQ"></div>
        <script src="//embed.typeform.com/next/embed.js" is:inline></script>
      </div>
    </div>
  </section>

  <!-- Problem Section -->
  <section class="problem-section" id="challenge">
    <div class="container">
      <div class="problem-card">
        <span class="section-badge">The Challenge</span>
        <h2>The problem isn't your accommodation. It's the model.</h2>
        <p>
          Empty months, fixed costs, growing competition and dependence on peak season are part of
          the reality for many hostels and guesthouses in Portugal. Even with a great product,
          seasonality and day-to-day management make income unpredictable and the business
          excessively owner-dependent.
        </p>
        <p class="highlight-text">
          With Host Wise's Guaranteed Income model, occupancy may change — <strong>your rent won't.</strong>
        </p>
      </div>
    </div>
  </section>

  <!-- Benefit 1 -->
  <section class="benefit-section">
    <div class="container">
      <div class="benefit-grid">
        <div class="benefit-image">
          <Image src={hostelArvore} alt="Host Wise Árvore - Guesthouse" loading="lazy" />
        </div>
        <div class="benefit-content">
          <span class="section-badge">Financial Stability</span>
          <h2>Protect Yourself from Seasonality and Receive a Fixed Rent Every Month</h2>
          <p>
            Lack of occupancy is no longer a problem. With Host Wise's guaranteed income model,
            you receive the same amount every month, whether it's peak or low season.
          </p>
          <a href="#form" class="btn-secondary-cta">
            How much is my accommodation worth?
            <span class="btn-arrow" set:html={iconMap.chevronRight} />
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Benefit 2 -->
  <section class="benefit-section benefit-reversed">
    <div class="container">
      <div class="benefit-grid">
        <div class="benefit-image">
          <Image src={portoLoungeRefeitorio} alt="Porto Lounge Hostel - Dining Area" loading="lazy" />
        </div>
        <div class="benefit-content">
          <span class="section-badge">Financial Freedom</span>
          <h2>Gain Cash Flow to Pursue Other Investments</h2>
          <p>
            Host Wise provides you with all the bank guarantees to pursue other investments,
            while we manage your accommodation and guarantee your financial stability.
          </p>
          <a href="#form" class="btn-secondary-cta">
            I'd Like to Receive a Proposal
            <span class="btn-arrow" set:html={iconMap.chevronRight} />
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Features Section -->
  <section class="features-section">
    <div class="container">
      <div class="features-header">
        <span class="section-badge">Benefits</span>
        <h2>What Do I Get?</h2>
      </div>
      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon" set:html={iconMap.euro} />
          <h3>Fixed Rent Every Month</h3>
          <p>Receive an agreed monthly amount upfront, regardless of your accommodation's occupancy.</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon" set:html={iconMap.integratedManagement} />
          <h3>A Business Running Without Worries</h3>
          <p>We handle bookings, pricing, staff, cleaning, maintenance and guest support.</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon" set:html={iconMap.clock} />
          <h3>More Time for Yourself</h3>
          <p>Your hostel stops being a full-time job and becomes a financial asset.</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon" set:html={iconMap.trendingUp} />
          <h3>Cash Flow for Future Investments</h3>
          <p>Use the predictable income to invest in other projects, real estate or personal.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Properties Section -->
  <section class="properties-section">
    <div class="container">
      <div class="properties-header">
        <span class="section-badge">Portfolio</span>
        <h2>Discover Some of Our Guaranteed Income Properties</h2>
      </div>
      <div class="properties-grid">
        <div class="property-card">
          <div class="property-image">
            <Image src={hostelPortoLounge} alt="Porto Lounge Hostel" loading="lazy" />
          </div>
          <div class="property-content">
            <h3>Porto Lounge Hostel</h3>
            <span class="property-location">
              <span class="location-icon" set:html={iconMap.location} />
              Porto
            </span>
            <p>
              Right in the heart of Porto, this hostel combines 19th-century architecture with
              a contemporary interior adapted to the needs of solo travellers, families and groups
              of friends who want to explore the Unvanquished City.
            </p>
          </div>
        </div>

        <div class="property-card">
          <div class="property-image">
            <Image src={arvoreVistaAerea} alt="Host Wise Árvore - Aerial View" loading="lazy" />
          </div>
          <div class="property-content">
            <h3>Host Wise Árvore</h3>
            <span class="property-location">
              <span class="location-icon" set:html={iconMap.location} />
              Vila do Conde
            </span>
            <p>
              For sea breeze lovers, Host Wise Árvore is just a few metres from Azurara Beach
              and a short walk from Mindelo Beach. If the north wind doesn't let up, there's a
              swimming pool and jacuzzi to satisfy the desire for water.
            </p>
          </div>
        </div>

        <div class="property-card">
          <div class="property-image">
            <Image src={hostelBurroVille} alt="Burro Ville" loading="lazy" />
          </div>
          <div class="property-content">
            <h3>Burro Ville</h3>
            <span class="property-location">
              <span class="location-icon" set:html={iconMap.location} />
              Portimão, Algarve
            </span>
            <p>
              If you want to escape the crowds that flood the Algarve but still enjoy the mild
              weather, Burro Ville is the perfect choice. Just 17km from Portimão city centre,
              it's an ideal spot for a couple's getaway.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ Section -->
  <FAQSection
    badgeLabel="FAQ"
    title="We Answer Your Questions"
    subtitle="Everything you need to know about managing your hostel or guesthouse with Host Wise."
    categories={faqCategories}
  />

  <!-- Typeform Embed (after FAQs) -->
  <section class="typeform-section" id="form-final">
    <div class="container">
      <div class="typeform-header">
        <span class="section-badge">Get Started</span>
        <h2>Ready to Hear Our Proposal?</h2>
        <p>
          Turn your hostel or guesthouse into an asset that generates predictable income, stress-free
          and without depending on occupancy.
        </p>
      </div>
      <div class="form-container">
        <div data-tf-live="01J881SMFDDP5K5A65ZF2F7XEQ"></div>
      </div>
    </div>
  </section>
</LayoutAd>

<style>
  /* Container */
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
  }

  /* Section Badge */
  .section-badge {
    display: inline-block;
    background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
    color: white;
    padding: 8px 20px;
    border-radius: 30px;
    font-weight: 700;
    font-size: 0.85rem;
    margin-bottom: 20px;
    letter-spacing: 0.02em;
  }

  /* Typeform Section */
  .typeform-section {
    padding-top: 120px;
    padding-bottom: 120px;
    text-align: center;
    background: linear-gradient(135deg, #F8FCFF 0%, #E5F4FE 100%);
  }

  .typeform-header {
    text-align: center;
    margin-bottom: 40px;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .typeform-header h2 {
    font-size: 2.5rem;
    font-weight: 800;
    color: var(--primary);
    line-height: 1.2;
    margin-bottom: 16px;
    letter-spacing: -0.02em;
  }

  .typeform-header p {
    font-size: 1.15rem;
    line-height: 1.7;
    color: var(--text);
    opacity: 0.85;
  }

  .form-container {
    background: white;
    border-radius: 20px;
    padding: 40px;
    box-shadow: 0 10px 40px rgba(12, 68, 99, 0.1);
    min-height: 600px;
    max-width: 800px;
    margin: 0 auto;
  }

  /* Problem Section */
  .problem-section {
    padding: 100px 20px;
    background: white;
  }

  .problem-card {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
  }

  .problem-card h2 {
    font-size: 2.75rem;
    font-weight: 800;
    color: var(--primary);
    line-height: 1.15;
    margin-bottom: 28px;
    letter-spacing: -0.02em;
  }

  .problem-card p {
    font-size: 1.15rem;
    line-height: 1.8;
    color: var(--text);
    opacity: 0.85;
  }

  .highlight-text {
    margin-top: 24px;
    font-size: 1.25rem !important;
    opacity: 1 !important;
    color: var(--primary) !important;
    font-weight: 600;
  }

  /* Benefit Sections */
  .benefit-section {
    padding: 100px 20px;
    background: var(--gray-light);
  }

  .benefit-section.benefit-reversed {
    background: white;
  }

  .benefit-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: center;
  }

  .benefit-reversed .benefit-grid {
    direction: rtl;
  }

  .benefit-reversed .benefit-grid > * {
    direction: ltr;
  }

  .benefit-image img {
    width: 100%;
    border-radius: 20px;
    box-shadow: 0 20px 60px rgba(12, 68, 99, 0.15);
  }

  .benefit-content h2 {
    font-size: 2.25rem;
    font-weight: 800;
    color: var(--primary);
    line-height: 1.2;
    margin-bottom: 20px;
  }

  .benefit-content p {
    font-size: 1.1rem;
    line-height: 1.8;
    color: var(--text);
    opacity: 0.85;
    margin-bottom: 32px;
  }

  .btn-secondary-cta {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    color: var(--secondary);
    font-weight: 700;
    font-size: 1.05rem;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .btn-secondary-cta:hover {
    gap: 14px;
    color: var(--primary);
  }

  .btn-arrow {
    width: 20px;
    height: 20px;
    display: flex;
    align-items: center;
    transition: transform 0.3s ease;
  }

  .btn-arrow :global(svg) {
    width: 100%;
    height: 100%;
  }

  .btn-secondary-cta:hover .btn-arrow {
    transform: translateX(4px);
  }

  /* Features Section */
  .features-section {
    padding: 100px 20px;
    background: white;
  }

  .features-header {
    text-align: center;
    margin-bottom: 60px;
  }

  .features-header h2 {
    font-size: 2.75rem;
    font-weight: 800;
    color: var(--primary);
    letter-spacing: -0.02em;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 32px;
  }

  .feature-card {
    background: var(--gray-light);
    padding: 40px 28px;
    border-radius: 20px;
    text-align: center;
    border: 2px solid transparent;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 4px 20px rgba(12, 68, 99, 0.06);
  }

  .feature-card:hover {
    transform: translateY(-6px);
    border-color: var(--secondary);
    box-shadow: 0 16px 40px rgba(12, 68, 99, 0.12);
  }

  .feature-icon {
    width: 48px;
    height: 48px;
    color: var(--secondary);
    margin: 0 auto 20px;
  }

  .feature-icon :global(svg) {
    width: 100%;
    height: 100%;
  }

  .feature-card h3 {
    font-size: 1.15rem;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 12px;
    line-height: 1.3;
  }

  .feature-card p {
    font-size: 0.95rem;
    line-height: 1.7;
    color: var(--text);
    opacity: 0.8;
  }

  /* Properties Section */
  .properties-section {
    padding: 100px 20px;
    background: var(--gray-light);
  }

  .properties-header {
    text-align: center;
    margin-bottom: 60px;
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
  }

  .properties-header h2 {
    font-size: 2.5rem;
    font-weight: 800;
    color: var(--primary);
    line-height: 1.2;
    letter-spacing: -0.02em;
  }

  .properties-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
  }

  .property-card {
    background: white;
    border-radius: 20px;
    overflow: hidden;
    border: 2px solid transparent;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 4px 20px rgba(12, 68, 99, 0.06);
  }

  .property-card:hover {
    transform: translateY(-6px);
    border-color: var(--secondary);
    box-shadow: 0 16px 40px rgba(12, 68, 99, 0.12);
  }

  .property-image {
    overflow: hidden;
    height: 240px;
  }

  .property-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
  }

  .property-card:hover .property-image img {
    transform: scale(1.05);
  }

  .property-content {
    padding: 28px;
  }

  .property-content h3 {
    font-size: 1.3rem;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 8px;
  }

  .property-location {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 0.9rem;
    color: var(--secondary);
    font-weight: 600;
    margin-bottom: 14px;
  }

  .location-icon {
    width: 16px;
    height: 16px;
  }

  .location-icon :global(svg) {
    width: 100%;
    height: 100%;
  }

  .property-content p {
    font-size: 0.95rem;
    line-height: 1.7;
    color: var(--text);
    opacity: 0.8;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .features-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .properties-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .properties-grid .property-card:last-child {
      grid-column: 1 / -1;
      max-width: 500px;
      margin: 0 auto;
    }
  }

  @media (max-width: 768px) {
    .typeform-section,
    .problem-section,
    .benefit-section,
    .features-section,
    .properties-section {
      padding: 80px 20px;
    }

    .typeform-header h2 {
      font-size: 2rem;
    }

    .form-container {
      padding: 24px;
      min-height: 500px;
    }

    .problem-card h2 {
      font-size: 2rem;
    }

    .benefit-grid {
      grid-template-columns: 1fr;
      gap: 40px;
    }

    .benefit-reversed .benefit-grid {
      direction: ltr;
    }

    .benefit-content h2 {
      font-size: 1.75rem;
    }

    .features-header h2 {
      font-size: 2rem;
    }

    .features-grid {
      grid-template-columns: 1fr;
      gap: 20px;
    }

    .properties-header h2 {
      font-size: 1.75rem;
    }

    .properties-grid {
      grid-template-columns: 1fr;
    }

    .properties-grid .property-card:last-child {
      max-width: none;
    }
  }

  @media (max-width: 480px) {
    .typeform-header h2 {
      font-size: 1.75rem;
    }

    .problem-card h2 {
      font-size: 1.75rem;
    }

    .benefit-content h2 {
      font-size: 1.5rem;
    }
  }
</style>
