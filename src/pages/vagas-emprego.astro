---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Vagas de Emprego - Host Wise | Oportunidades de Carreira">
  <!-- Hero Section -->
  <section class="jobs-hero">
    <div class="container">
      <div class="hero-content">
        <div class="hero-badge">
          <span>Oportunidades</span>
        </div>
        <h1 class="hero-title">Explora as Nossas Oportunidades</h1>
        <p class="hero-description">
          Descobre as posições disponíveis e junta-te a uma equipa onde o teu talento faz a diferença.
          Todas as vagas estão abertas para candidaturas.
        </p>
      </div>
    </div>
  </section>

  <!-- Recruitee Jobs Widget Section -->
  <section class="jobs-section">
    <div class="container">
      <div id="recruitee-careers"></div>
      <script type="text/javascript" is:inline>
        var rtscript = document.createElement('script');
        rtscript.type = 'text/javascript';
        rtscript.onload = function() {
          var widget = new RTWidget({
            "companies": [
              87270
            ],
            "detailsMode": "popup",
            "language": "en",
            "departmentsFilter": [],
            "tagsFilter": [],
            "themeVars": {
              "primary": "#0c4463",
              "secondary": "#232323",
              "text": "#232323",
              "textDark": "#121317",
              "fontFamily": "\"Helvetica Neue\", Helvetica, Arial, \"Lucida Grande\", sans-serif;",
              "baseFontSize": "16px"
            },
            "flags": {
              "showLocation": true,
              "showCountry": false,
              "showCity": true,
              "showState": false,
              "showLocationName": false,
              "showWorkModel": true,
              "groupByLocation": false,
              "groupByState": false,
              "groupByDepartment": true,
              "groupByCompany": false
            }
          })
        };
        rtscript.src = 'https://jobs-widget.recruiteecdn.com/widget.js';
        document.body.appendChild(rtscript);
      </script>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="bottom-cta-section">
    <div class="container">
      <div class="cta-box">
        <h2>Não encontraste a vaga ideal?</h2>
        <p>
          Envia-nos o teu CV espontâneo. Estamos sempre à procura de talento excepcional para juntar à nossa equipa.
        </p>
        <a href="mailto:recrutamento@hostwise.pt" class="btn-cta-email">
          Envia o Teu CV
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" width="20" height="20">
            <path d="M20 4H4c-1.1 0-1.99.9-1.99 2L2 18c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4l-8 5-8-5V6l8 5 8-5v2z"/>
          </svg>
        </a>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* Hero Section */
  .jobs-hero {
    padding: 180px 20px 80px;
    background: linear-gradient(135deg, #F8FCFF 0%, #E5F4FE 100%);
    text-align: center;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .hero-content {
    max-width: 800px;
    margin: 0 auto;
  }

  .hero-badge {
    display: inline-block;
    background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
    color: white;
    padding: 10px 24px;
    border-radius: 30px;
    font-weight: 700;
    font-size: 0.9rem;
    margin-bottom: 32px;
    animation: fadeInDown 0.6s ease-out;
  }

  @keyframes fadeInDown {
    from {
      opacity: 0;
      transform: translateY(-20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .hero-title {
    font-size: 4rem;
    font-weight: 900;
    color: var(--primary);
    line-height: 1.1;
    margin-bottom: 24px;
    letter-spacing: -0.02em;
    animation: fadeInUp 0.8s ease-out 0.2s backwards;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .hero-description {
    font-size: 1.25rem;
    line-height: 1.7;
    color: var(--text);
    opacity: 0.85;
    animation: fadeInUp 0.8s ease-out 0.4s backwards;
  }

  /* Jobs Section */
  .jobs-section {
    padding: 100px 20px;
    background: white;
  }


  /* Bottom CTA Section */
  .bottom-cta-section {
    padding: 100px 20px;
    background: white;
  }

  .cta-box {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
    background: white;
    border: 2px solid var(--gray-light);
    border-radius: 24px;
    padding: 60px 40px;
    box-shadow: 0 8px 32px rgba(12, 68, 99, 0.1);
  }

  .cta-box h2 {
    font-size: 2.5rem;
    font-weight: 800;
    color: var(--primary);
    margin-bottom: 20px;
    line-height: 1.2;
  }

  .cta-box p {
    font-size: 1.2rem;
    line-height: 1.7;
    color: var(--text);
    opacity: 0.85;
    margin-bottom: 36px;
  }

  .btn-cta-email {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    padding: 18px 36px;
    background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
    color: white;
    border-radius: 12px;
    font-size: 1.15rem;
    font-weight: 700;
    text-decoration: none;
    transition: all 0.3s ease;
    box-shadow: 0 6px 20px rgba(12, 68, 99, 0.25);
  }

  .btn-cta-email:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 32px rgba(12, 68, 99, 0.35);
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .jobs-hero {
      padding: 160px 20px 60px;
    }

    .hero-title {
      font-size: 3rem;
    }
  }

  @media (max-width: 768px) {
    .jobs-hero {
      padding: 140px 20px 50px;
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .hero-description {
      font-size: 1.1rem;
    }

    .jobs-section,
    .bottom-cta-section {
      padding: 80px 20px;
    }

    .cta-box {
      padding: 50px 30px;
    }

    .cta-box h2 {
      font-size: 2rem;
    }

    .btn-cta-email {
      width: 100%;
      justify-content: center;
    }
  }

  @media (max-width: 480px) {
    .hero-title {
      font-size: 2rem;
    }

    .cta-box h2 {
      font-size: 1.75rem;
    }

    .cta-box p {
      font-size: 1.05rem;
    }
  }
</style>
