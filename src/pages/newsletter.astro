---
import Layout from '../layouts/Layout.astro';
import { iconMap } from '../icons/iconMap';
---

<Layout title="Newsletter Host Wise | Fique a Par das Novidades">
  <!-- Hero Section -->
  <section class="newsletter-hero">
    <div class="hero-background">
      <div class="gradient-orb gradient-orb-1"></div>
      <div class="gradient-orb gradient-orb-2"></div>
    </div>

    <div class="container">
      <div class="hero-content">
        <div class="hero-badge">
          <span>Newsletter Host Wise</span>
        </div>

        <h1 class="hero-heading">Fique a Par das Novidades</h1>

        <p class="hero-description">
          Subscreva a nossa newsletter e receba as últimas notícias, dicas de gestão de propriedades
          e ofertas exclusivas diretamente no seu email.
        </p>
      </div>
    </div>
  </section>

  <!-- Newsletter Form Section -->
  <section class="newsletter-form-section">
    <div class="container">
      <div class="form-wrapper">
        <div class="beehiiv-embed">
          <iframe
            src="https://embeds.beehiiv.com/630c0a33-b0a9-49e9-b477-b8b0740d9cc1"
            data-test-id="beehiiv-embed"
            width="100%"
            height="320"
            frameborder="0"
            scrolling="no"
            style="border-radius: 4px; border: 2px solid #e5e7eb; margin: 0; background-color: transparent;"
          ></iframe>
        </div>
      </div>
    </div>
  </section>

  <!-- Benefits Section -->
  <section class="benefits-section">
    <div class="container">
      <div class="section-header">
        <h2>O Que Vai Receber</h2>
        <p>Conteúdo valioso diretamente na sua caixa de entrada</p>
      </div>

      <div class="benefits-grid">
        <div class="benefit-card">
          <div class="benefit-icon" set:html={iconMap.home} />
          <h3>Insights do Mercado</h3>
          <p>Análises e tendências do setor de alojamento local em Portugal</p>
        </div>

        <div class="benefit-card">
          <div class="benefit-icon" set:html={iconMap.lightbulb} />
          <h3>Dicas de Gestão</h3>
          <p>Estratégias práticas para maximizar a rentabilidade da sua propriedade</p>
        </div>

        <div class="benefit-card">
          <div class="benefit-icon" set:html={iconMap.gift} />
          <h3>Ofertas Exclusivas</h3>
          <p>Acesso antecipado a promoções e condições especiais</p>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* Hero Section */
  .newsletter-hero {
    position: relative;
    padding: 200px 20px 120px;
    background: linear-gradient(135deg, var(--blue-light) 0%, rgba(92, 176, 214, 0.05) 100%);
    overflow: hidden;
  }

  .hero-background {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .gradient-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.4;
  }

  .gradient-orb-1 {
    top: -100px;
    right: -100px;
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(92, 176, 214, 0.4) 0%, transparent 70%);
  }

  .gradient-orb-2 {
    bottom: -150px;
    left: -150px;
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(12, 68, 99, 0.3) 0%, transparent 70%);
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  .hero-content {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
  }

  .hero-badge {
    display: inline-block;
    background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
    color: white;
    padding: 10px 24px;
    border-radius: 30px;
    font-weight: 700;
    font-size: 0.9rem;
    margin-bottom: 32px;
    animation: fadeInDown 0.6s ease-out;
  }

  @keyframes fadeInDown {
    from {
      opacity: 0;
      transform: translateY(-20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .hero-heading {
    font-size: 4.5rem;
    font-weight: 900;
    background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1.1;
    margin-bottom: 32px;
    letter-spacing: -0.02em;
    animation: fadeInUp 0.8s ease-out 0.2s backwards;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .hero-description {
    font-size: 1.35rem;
    line-height: 1.7;
    color: var(--text);
    opacity: 0.85;
    animation: fadeInUp 0.8s ease-out 0.4s backwards;
  }

  /* Newsletter Form Section */
  .newsletter-form-section {
    padding: 100px 20px;
    background: white;
  }

  .form-wrapper {
    max-width: 700px;
    margin: 0 auto;
  }

  .form-header {
    text-align: center;
    margin-bottom: 48px;
  }

  .form-icon {
    width: 80px;
    height: 80px;
    margin: 0 auto 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, var(--blue-light) 0%, rgba(92, 176, 214, 0.05) 100%);
    border-radius: 20px;
    color: var(--secondary);
  }

  .form-icon svg {
    width: 40px;
    height: 40px;
  }

  .form-header h2 {
    font-size: 2.5rem;
    font-weight: 800;
    color: var(--primary);
    margin-bottom: 16px;
    line-height: 1.2;
  }

  .form-header p {
    font-size: 1.2rem;
    color: var(--text);
    opacity: 0.85;
    line-height: 1.7;
  }

  .beehiiv-embed {
    animation: fadeIn 0.8s ease-out 0.6s backwards;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  /* Benefits Section */
  .benefits-section {
    padding: 100px 20px;
    background: linear-gradient(180deg, white 0%, var(--blue-light) 100%);
  }

  .section-header {
    text-align: center;
    max-width: 800px;
    margin: 0 auto 60px;
  }

  .section-header h2 {
    font-size: 3rem;
    font-weight: 800;
    color: var(--primary);
    margin-bottom: 16px;
    line-height: 1.2;
  }

  .section-header p {
    font-size: 1.2rem;
    color: var(--text);
    opacity: 0.85;
    line-height: 1.7;
  }

  .benefits-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 40px;
    max-width: 1200px;
    margin: 0 auto;
  }

  .benefit-card {
    background: white;
    border: 2px solid var(--gray-light);
    border-radius: 20px;
    padding: 40px 32px;
    text-align: center;
    transition: all 0.3s ease;
  }

  .benefit-card:hover {
    border-color: var(--secondary);
    transform: translateY(-8px);
    box-shadow: 0 12px 40px rgba(12, 68, 99, 0.15);
  }

  .benefit-icon {
    width: 64px;
    height: 64px;
    margin: 0 auto 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--blue-light);
    border-radius: 16px;
    color: var(--secondary);
    padding: 10px;
  }

  .benefit-icon svg {
    width: 32px;
    height: 32px;
  }

  .benefit-card h3 {
    font-size: 1.4rem;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 12px;
    line-height: 1.3;
  }

  .benefit-card p {
    font-size: 1.05rem;
    line-height: 1.7;
    color: var(--text);
    opacity: 0.85;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .newsletter-hero {
      padding: 160px 20px 100px;
    }

    .hero-heading {
      font-size: 3.5rem;
    }

    .benefits-grid {
      grid-template-columns: 1fr;
      gap: 32px;
    }
  }

  @media (max-width: 768px) {
    .newsletter-hero {
      padding: 140px 20px 80px;
    }

    .hero-heading {
      font-size: 2.5rem;
    }

    .hero-description {
      font-size: 1.15rem;
    }

    .newsletter-form-section,
    .benefits-section {
      padding: 80px 20px;
    }

    .form-header h2,
    .section-header h2 {
      font-size: 2rem;
    }

    .form-header p,
    .section-header p {
      font-size: 1.05rem;
    }
  }

  @media (max-width: 480px) {
    .hero-heading {
      font-size: 2rem;
    }

    .form-icon {
      width: 64px;
      height: 64px;
    }

    .form-icon svg {
      width: 32px;
      height: 32px;
    }
  }
</style>
