---
import Layout from '../layouts/Layout.astro';
import { iconMap } from '../icons/iconMap';
---

<Layout title="Contacto - Host Wise">
  <section class="contact-hero">
    <div class="container">
      <span class="badge">Contacto</span>
      <h1>Fale Connosco</h1>
      <p>Estamos disponíveis para esclarecer as suas dúvidas e apresentar uma proposta personalizada para o seu imóvel.</p>
    </div>
  </section>

  <section class="contact-section">
    <div class="container">
      <div class="contact-grid">
        <!-- Email Card -->
        <div class="contact-card">
          <div class="card-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
              <polyline points="22,6 12,13 2,6"/>
            </svg>
          </div>
          <h2>Email</h2>
          <p>Para questões comerciais, propostas ou parcerias.</p>
          <a href="mailto:comercial@hostwise.pt" class="contact-link">
            <span>comercial@hostwise.pt</span>
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <line x1="5" y1="12" x2="19" y2="12"/>
              <polyline points="12 5 19 12 12 19"/>
            </svg>
          </a>
        </div>

        <!-- Phone Card -->
        <div class="contact-card">
          <div class="card-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
            </svg>
          </div>
          <h2>Telefone</h2>
          <p>Ligue-nos para falar diretamente com a nossa equipa.</p>
          <a href="tel:+351308804682" class="contact-link">
            <span>308 804 682</span>
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <line x1="5" y1="12" x2="19" y2="12"/>
              <polyline points="12 5 19 12 12 19"/>
            </svg>
          </a>
        </div>

        
      </div>

      
  </section>

</Layout>

<style>
  /* Hero */
  .contact-hero {
    padding: 120px 20px 80px;
    background: linear-gradient(180deg, var(--blue-light) 0%, white 100%);
    text-align: center;
  }

  .contact-hero .container {
    max-width: 800px;
    margin: 0 auto;
  }

  .badge {
    display: inline-block;
    background: rgba(92, 176, 214, 0.15);
    color: var(--secondary);
    padding: 8px 20px;
    border-radius: 24px;
    font-weight: 700;
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 24px;
  }

  .contact-hero h1 {
    font-size: 3.5rem;
    font-weight: 800;
    color: var(--primary);
    margin-bottom: 20px;
    letter-spacing: -0.02em;
  }

  .contact-hero p {
    font-size: 1.25rem;
    color: var(--text);
    line-height: 1.7;
    opacity: 0.85;
  }

  /* Contact Section */
  .contact-section {
    padding: 80px 20px 100px;
    background: white;
  }

  .contact-section .container {
    max-width: 1100px;
    margin: 0 auto;
  }

  .contact-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 32px;
    margin-bottom: 60px;
  }

  .contact-card {
    background: white;
    border-radius: 24px;
    padding: 40px 32px;
    border: 2px solid var(--gray-light);
    transition: all 0.3s ease;
    text-align: center;
  }

  .contact-card:hover {
    border-color: var(--secondary);
    box-shadow: 0 16px 48px rgba(12, 68, 99, 0.1);
    transform: translateY(-4px);
  }

  .card-icon {
    width: 72px;
    height: 72px;
    background: var(--blue-light);
    border-radius: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 24px;
    color: var(--secondary);
    padding: 18px;
  }

  .card-icon svg {
    width: 100%;
    height: 100%;
  }

  .contact-card h2 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 12px;
  }

  .contact-card p {
    font-size: 1rem;
    color: var(--text);
    line-height: 1.6;
    opacity: 0.8;
    margin-bottom: 24px;
  }

  .contact-link {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--secondary);
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .contact-link:hover {
    color: var(--primary);
    gap: 14px;
  }

  .contact-link svg {
    width: 20px;
    height: 20px;
    transition: transform 0.3s ease;
  }

  .contact-link:hover svg {
    transform: translateX(4px);
  }

  .btn-support {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    padding: 16px 28px;
    background: #25D366;
    color: white;
    border-radius: 12px;
    font-size: 1rem;
    font-weight: 700;
    text-decoration: none;
    transition: all 0.3s ease;
    box-shadow: 0 4px 16px rgba(37, 211, 102, 0.3);
  }

  .btn-support:hover {
    background: #20bd5a;
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(37, 211, 102, 0.4);
  }

  /* Info Section */
  .info-section {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 32px;
  }

  .info-card {
    background: var(--blue-light);
    border-radius: 20px;
    padding: 32px;
    border: 2px solid rgba(92, 176, 214, 0.15);
  }

  .info-card h3 {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 20px;
  }

  .info-card p {
    font-size: 1rem;
    color: var(--text);
    line-height: 1.7;
    opacity: 0.85;
    margin: 0;
  }

  .schedule {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .schedule-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 12px 16px;
    background: white;
    border-radius: 10px;
  }

  .schedule-item .day {
    font-weight: 600;
    color: var(--text);
  }

  .schedule-item .time {
    font-weight: 700;
    color: var(--primary);
  }

  /* CTA Section */
  .cta-section {
    padding: 100px 20px;
    background: linear-gradient(180deg, white 0%, var(--blue-light) 100%);
  }

  .cta-section .container {
    max-width: 900px;
    margin: 0 auto;
  }

  .cta-card {
    background: white;
    border-radius: 28px;
    padding: 60px;
    text-align: center;
    box-shadow: 0 16px 64px rgba(12, 68, 99, 0.12);
    border: 2px solid var(--gray-light);
    position: relative;
    overflow: hidden;
  }

  .cta-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 6px;
    background: linear-gradient(90deg, var(--primary) 0%, var(--secondary) 100%);
  }

  .cta-card h2 {
    font-size: 2rem;
    font-weight: 800;
    color: var(--primary);
    margin-bottom: 16px;
    letter-spacing: -0.02em;
  }

  .cta-card p {
    font-size: 1.15rem;
    color: var(--text);
    line-height: 1.7;
    opacity: 0.85;
    margin-bottom: 32px;
  }

  .btn-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 18px 40px;
    background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
    color: white;
    border-radius: 14px;
    font-size: 1.15rem;
    font-weight: 700;
    text-decoration: none;
    transition: all 0.3s ease;
    box-shadow: 0 8px 28px rgba(12, 68, 99, 0.3);
  }

  .btn-cta:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 40px rgba(12, 68, 99, 0.4);
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .contact-grid {
      grid-template-columns: 1fr;
      max-width: 500px;
      margin-left: auto;
      margin-right: auto;
    }

    .info-section {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 768px) {
    .contact-hero {
      padding: 100px 20px 60px;
    }

    .contact-hero h1 {
      font-size: 2.5rem;
    }

    .contact-hero p {
      font-size: 1.1rem;
    }

    .contact-section {
      padding: 60px 20px 80px;
    }

    .contact-card {
      padding: 32px 24px;
    }

    .card-icon {
      width: 60px;
      height: 60px;
    }

    .contact-card h2 {
      font-size: 1.35rem;
    }

    .cta-section {
      padding: 80px 20px;
    }

    .cta-card {
      padding: 40px 28px;
    }

    .cta-card h2 {
      font-size: 1.75rem;
    }

    .cta-card p {
      font-size: 1.05rem;
    }

    .btn-cta {
      width: 100%;
      padding: 16px 32px;
    }
  }

  @media (max-width: 480px) {
    .contact-hero h1 {
      font-size: 2rem;
    }

    .cta-card h2 {
      font-size: 1.5rem;
    }

    .info-card {
      padding: 24px;
    }

    .schedule-item {
      flex-direction: column;
      gap: 4px;
      text-align: center;
    }
  }
</style>
